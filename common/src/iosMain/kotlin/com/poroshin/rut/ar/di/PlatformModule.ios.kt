package com.poroshin.rut.ar.di

import org.koin.core.module.Module
import org.koin.dsl.module

/**
 * iOS-specific DI module: add iOS drivers/clients here.
 */
val iosPlatformModule: Module = module {
    // placeholder for iOS-specific bindings (e.g., SQLDelight native driver, Ktor Darwin engine)
}


